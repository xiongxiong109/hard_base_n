// 数据结构 列表

class List {
    constructor(props) {
        this._dataStore = []; // 列表数据
        this._curIdx = 0; // 当前索引
    }
    first() { // 访问列表的头部元素

    }
    last() { // 访问列表的最后一个元素

    }
    insertBefore(index = 0) { // 在列表的某一位置插入元素
        
    }
    prev() { // 列表的上一个元素

    }
    next() { // 列表的下一个元素

    }
    toString() { // 返回列表本身

    }
}

module.exports = List